#include <stdio.h>
#include "main.h"

/**
 * mul - checks for digit
 * @a: 1st input for func
 * @b: 2nd input of func
 * Return: multiplication result(res)
 */

int mul(int a, int b)
{
	int res;

	res = 0;
	res = (a * b);
	return (res);
}
