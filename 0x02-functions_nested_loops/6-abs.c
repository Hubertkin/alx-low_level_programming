#include <stdlib.>
#include "main.h"

/**
 * _abs - computes absolute value of input
 * @c: parameter for func _abs
 * Return: absolute value of input
 */

int _abs(int c)
{
	int i;

	i = abs(c);
	return (i);
}

