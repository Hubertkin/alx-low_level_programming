#include <stdio.h>
#include <string.h>

/**
 * _strlen - computes length of string
 * @s: takes address of input
 * Return: length of string
 */

int _strlen(char *s)
{
	int d;

	d = strlen(*s);
	return (d);
}
